# --- Núcleo del servidor ---
fastapi==0.115.6          # Framework web async
uvicorn[standard]==0.34.0 # Servidor ASGI para correr FastAPI
python-multipart==0.0.20  # Necesario para que FastAPI lea los POST de Twilio (form-urlencoded)
pydantic-settings==2.7.1  # Carga de configuración desde variables de entorno (.env)

# --- Mensajería ---
twilio==9.4.1             # SDK oficial de Twilio (envío saliente + generación de TwiML)

# --- IA (opcional en Fase 1, ya preparado para Fase 2) ---
anthropic==0.49.0         # SDK oficial de Claude para el Agente de IA por marca

# --- Persistencia (opcional: sólo si vas a usar Supabase) ---
supabase==2.10.0          # SDK oficial de Supabase (persistencia server-side)


# --- Scheduler (envios automaticos) ---
APScheduler==3.10.4       # Tareas programadas dentro de la app (encuestas a 48hs)

# --- HTTP (para la Content API de Twilio: botones) ---
requests==2.32.3        # Crear plantillas de respuesta rapida (botones) via API
